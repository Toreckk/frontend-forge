# Example Challenge

This is a sample folder structure to demonstrate where future challenges will live.

## Structure

- Each challenge should have its own folder in `src/challenges/`
- Use numbered prefixes for ordering (e.g., `01-accordion`, `02-tooltip`)
- Include all component files, styles, and assets within the challenge folder
